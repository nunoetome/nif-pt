"""scrapers — reservado para fallback HTML v0.4.

Futuro: scraping nif.pt HTML quando API falha.
Logging: TAG [scrape]
"""

import logging

logger = logging.getLogger(__name__)
logger.debug("[scrape] scrapers package loaded")
